apiVersion: v1
kind: Service
metadata:
  name: node-hello-app-service
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: node-hello-app
  ports:
    - protocol: TCP
      port: 3001
      targetPort: 3000
