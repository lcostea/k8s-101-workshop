apiVersion: apps/v1
kind: Deployment
metadata:
  name: node-liveness-readiness
  labels:
    app: node-liveness-readiness
spec:
  replicas: 3
  selector:
    matchLabels:
      app: node-liveness-readiness
  template:
    metadata:
      labels:
        app: node-liveness-readiness
    spec:
      containers:
      - name: node-liveness-readiness
        image: lcostea/node-liveness-readiness:v0.1.0
        livenessProbe:
          httpGet:
            path: /healthy
            port: 3000
          initialDelaySeconds: 5 # default 0s (doesn't wait)
          periodSeconds: 5 # default is 10s
          timeoutSeconds: 1 # default 1s
          failureThreshold: 2 # default 3
          successThreshold: 1 # default 1
